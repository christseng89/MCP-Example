[project]
name = "fast-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "botocore>=1.40.6",
]

[dependency-groups]
dev = [
    "fast-agent",
]

[tool.uv.sources]
fast-agent = { workspace = true }
